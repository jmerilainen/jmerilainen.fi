{
    "name": "jmerilainen.fi",
    "version": "1.0.0",
    "scripts": {
        "build": "NODE_ENV=production npx tailwindcss -o public/styles.css --jit --purge='./public/**/*.html' --minify",
        "dev": "npx tailwindcss -o public/styles.css --jit --watch & npx serve public"
    },
    "license": "MIT"
}
